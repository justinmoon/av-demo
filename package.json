{
  "name": "av-demo-phase1-step2-claude",
  "version": "1.0.0",
  "description": "- plans/MOQ_MARMOT_AV_PLAN.md — Project plan (browser‑first: Text → Audio → Video) - MOQ_MARMOT_AV_SPEC.md — Spec for MoQ + Marmot A/V (auth, directory, AEAD, tracks) - MOQ_CHAT_SERVER.md — Binary Nostr relay (MoQ chat accelerator) design - NOSTR_AUTH.md — Self‑issued caps + write‑proof auth (JWT alternative)",
  "main": "index.js",
  "scripts": {
    "build:wasm": "wasm-pack build crates/marmot-chat --target web --out-dir ../../tests/pkg",
    "build:ui": "tsc --project apps/chat-ui/tsconfig.json --noEmit && esbuild apps/chat-ui/main.ts --bundle --format=esm --target=es2020 --sourcemap --outfile=apps/chat-ui/dist/main.js --log-level=info --external:../../tests/pkg/*",
    "build": "npm run build:wasm && npm run build:ui",
    "pretest": "npm run build",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "esbuild": "^0.25.10",
    "playwright": "^1.55.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@kixelated/moq": "^0.9.1",
    "nostr-tools": "^2.17.0"
  }
}
