{
  "name": "av-demo-phase1-step2-claude",
  "version": "1.0.0",
  "description": "- plans/MOQ_MARMOT_AV_PLAN.md — Project plan (browser‑first: Text → Audio → Video) - MOQ_MARMOT_AV_SPEC.md — Spec for MoQ + Marmot A/V (auth, directory, AEAD, tracks) - MOQ_CHAT_SERVER.md — Binary Nostr relay (MoQ chat accelerator) design - NOSTR_AUTH.md — Self‑issued caps + write‑proof auth (JWT alternative)",
  "main": "index.js",
  "scripts": {
    "build:wasm": "wasm-pack build crates/marmot-chat --target web --out-dir ../../tests/pkg",
    "build:ui": "tsc --project apps/chat-ui/tsconfig.json && node apps/chat-ui/esbuild.config.mjs && cp tests/pkg/marmot_chat_bg.wasm apps/chat-ui/dist/ && cp apps/chat-ui/index.html apps/chat-ui/dist/ && cp apps/chat-ui/styles.css apps/chat-ui/dist/",
    "build": "npm run build:wasm && npm run build:ui",
    "pretest": "npm run build",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@types/node": "^20.19.19",
    "esbuild": "^0.25.10",
    "esbuild-plugin-solid": "^0.5.0",
    "playwright": "^1.55.1",
    "solid-refresh": "^0.6.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@kixelated/moq": "^0.9.1",
    "@solidjs/router": "^0.13.6",
    "nostr-tools": "^2.17.0",
    "solid-js": "^1.9.9"
  }
}
